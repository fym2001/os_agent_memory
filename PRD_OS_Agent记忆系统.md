# 产品需求文档（PRD）
# OS Agent 记忆优化及高效应用研究

**题目编号：** XA-202612  
**发榜单位：** 麒麟软件有限公司  
**文档版本：** v1.0  
**最后更新：** 2026-06-13

---

## 目录

1. [项目背景与痛点](#1-项目背景与痛点)
2. [解决方案概述](#2-解决方案概述)
3. [详细功能列表](#3-详细功能列表)
4. [功能优先级（MVP 与后续版本）](#4-功能优先级)
5. [技术栈建议](#5-技术栈建议)
6. [非功能性需求](#6-非功能性需求)
7. [系统架构设计](#7-系统架构设计)
8. [API 接口设计](#8-api-接口设计)
9. [数据模型设计](#9-数据模型设计)
10. [评测指标与测试方案](#10-评测指标与测试方案)
11. [比赛评分要点对照](#11-比赛评分要点对照)
12. [提交物清单](#12-提交物清单)
13. [里程碑与时间规划](#13-里程碑与时间规划)

---

## 1. 项目背景与痛点

### 1.1 背景

基于麒麟操作系统的 OS Agent 是智能交互系统的核心组件，其记忆模块中的**偏好记忆**与**知识记忆**是支撑个性化服务适配、知识沉淀复用的核心载体。工具调用执行结果是两类记忆的核心数据源之一，此外记忆还来源于用户手动配置、跨场景行为数据等多类渠道。

### 1.2 核心痛点

| 痛点类别 | 具体问题 | 影响 |
|---|---|---|
| **工具结果结构化不足** | 工具调用执行结果数据质量参差不齐，缺乏统一格式 | 偏好提取不精准，知识沉淀效率低 |
| **用户行为捕捉不全面** | 跨场景行为数据覆盖不足，隐式偏好丢失 | 动态偏好提取偏差大 |
| **版本化管理混乱** | 偏好配置多版本并存，无法有效回溯和合并 | 版本化管理效率低 |
| **知识冲突处理滞后** | 新旧知识冲突检测不及时，冲突融合机制缺失 | 知识库质量下降，调用准确率低 |
| **关联检索精度有限** | 语义检索能力弱，召回率不达标 | 知识复用效率低，Agent 响应质量差 |
| **数据安全合规风险** | 敏感信息未过滤即存储，缺乏精准遗忘机制 | 隐私泄露风险，合规问题 |
| **端侧资源占用高** | 模型和索引体积大，检索延迟超标 | 麒麟系统端侧部署困难 |

### 1.3 目标用户

- 使用银河麒麟桌面操作系统的个人用户
- 办公自动化场景下的知识工作者
- 智能协作、个性化助手场景的企业用户

---

## 2. 解决方案概述

### 2.1 产品定位

> **面向 OS Agent 的端侧多源融合记忆管理系统**

系统通过标准化接口接收 Agent 运行轨迹，对偏好记忆和知识记忆进行精准捕捉、结构化整合、高效检索与安全管理，运行于银河麒麟桌面操作系统，适配端侧轻量化部署需求。

### 2.2 核心解决思路

```
多源数据接入
    ↓
数据清洗 & 质量校验 & 标准化
    ↓
偏好抽取 & 知识整合（含冲突处理）
    ↓
版本化存储（Markdown + SQLite + 向量索引）
    ↓
混合检索 & 记忆注入提示词
    ↓
自然语言遗忘 & 敏感信息过滤
    ↓
评测指标闭环验证
```

### 2.3 与 Agent 的协作模式

Memory 模块不主动工作，由 Agent 在三个固定时机调用：

- **任务开始前**：`retrieve` — 检索相关记忆，注入 LLM 提示词
- **工具调用后**：`ingest_event` — 记录工具调用事件
- **任务结束后**：`commit` — 提交完整 session trace，生成长期记忆

---

## 3. 详细功能列表

### 3.1 多源数据整合模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-01 | 统一数据接入 | 支持工具执行结果、用户原始输入、手动配置、跨场景行为数据的统一接入，提供标准化事件提交接口 |
| F-02 | 数据清洗 | 去除噪声、重复事件、无效字段；对工具调用结果做结构化解析 |
| F-03 | 格式标准化 | 将多源异构数据转换为统一的 `raw_event` schema，补全 event_id、timestamp、scene、source 等元数据 |
| F-04 | 数据质量校验 | 对置信度低、内容模糊的输入打低分或拦截，不写入候选记忆 |
| F-05 | 场景识别 | 自动识别当前事件所属场景（办公、会议、编程、文件管理等），支持多场景标签 |
| F-06 | 短/中/长期记忆兼容 | 支持与短期记忆（session 内存）、中期记忆（SQLite 近期事件）、长期记忆（Markdown + 向量库）的数据流转 |

### 3.2 偏好记忆动态捕捉模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-10 | 显式偏好抽取 | 从用户原始输入中识别明确偏好语句（"以后都用 Markdown""不要上传文件"），提取为结构化偏好条目 |
| F-11 | 隐式偏好挖掘 | 从高频工具调用行为、输出格式选择、文件路径规律中推断隐式偏好 |
| F-12 | 工具偏好分析 | 记录工具使用频率、成功率、响应时间、输出格式，形成工具选择偏好画像 |
| F-13 | 置信度评分 | 对每条候选偏好赋予置信度分值（0~1），多源证据融合提升置信度 |
| F-14 | 版本化管理 | 每次偏好更新生成新版本，保留历史版本，支持回溯查询 |
| F-15 | 跨场景偏好适配 | 相同用户在不同场景下的偏好可差异化存储，检索时按场景优先匹配 |
| F-16 | 偏好冲突检测 | 检测新旧偏好冲突（如"以前用 Word，现在改 PDF"），自动标记并按规则合并 |
| F-17 | 安全策略偏好 | 专项识别并强制存储安全类偏好（不上传文件、本地处理敏感文件等），检索时强制注入 |

### 3.3 知识记忆结构化整合模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-20 | 知识条目抽取 | 从 LLM plan、工具结果、最终回答中抽取可复用知识，包括工作流知识、历史案例、模板 |
| F-21 | 结构化存储 | 知识按类型（workflow / case / template / fact）分类存储，每条知识含 id、类型、场景、内容、置信度、来源、更新时间 |
| F-22 | 知识冲突处理 | 新知识入库前检测与已有知识的冲突；冲突处理策略：新覆盖旧 / 保留双版本 / 置信度加权合并 |
| F-23 | 关联检索优化 | 基于关键词 + 向量语义的混合检索，按相关性和置信度排序，返回 Top-K 条目 |
| F-24 | 知识复用模板 | 支持将高频工作流提取为可复用模板（如"会议纪要整理流程"），供 Agent 直接引用 |
| F-25 | 知识老化管理 | 超过时效阈值或低置信度的知识条目自动降权或标记为"待验证"状态 |

### 3.4 检索与记忆注入模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-30 | 语义检索 | 使用麒麟 embedding SDK 进行向量检索，支持语义相似度匹配 |
| F-31 | 关键词检索 | 基于 BM25 的关键词检索，作为语义检索的补充 |
| F-32 | 混合检索融合 | 将语义检索和关键词检索结果按权重融合，返回最优 Top-K |
| F-33 | 提示词注入 | 将检索到的偏好、知识、安全规则按模板拼装为 context prompt，注入 LLM |
| F-34 | 安全规则强制注入 | 安全类记忆不参与 Top-K 过滤，始终注入提示词 |
| F-35 | 检索缓存 | 对高频查询结果做本地缓存，降低检索延迟 |

### 3.5 敏感信息过滤与遗忘模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-40 | 敏感信息识别 | 在数据写入前识别并过滤身份证号、手机号、密码、密钥等敏感信息 |
| F-41 | 自然语言遗忘 | 解析自然语言遗忘指令（"忘掉我关于 Word 格式的偏好"），定位并删除或失效对应记忆条目 |
| F-42 | 精准遗忘 | 支持按 memory_id、关键词、时间范围、记忆类型精确指定遗忘范围 |
| F-43 | 遗忘审计日志 | 记录每次遗忘操作的指令、目标条目、操作时间，支持审计查询 |

### 3.6 评测与统计模块

| 功能 ID | 功能名称 | 功能描述 |
|---|---|---|
| F-50 | 指标统计接口 | 提供 `/memory/stats` 接口，输出偏好提取准确率、知识检索召回率、冲突处理正确率、检索响应时间 |
| F-51 | 标准化测试数据集 | 包含用户行为样本、工具结果样本、冲突样例、遗忘指令样例的标准化测试集 |
| F-52 | 对比实验框架 | 支持有/无记忆增强的 Agent 对比实验，量化记忆系统的提升效果 |
| F-53 | 测试报告生成 | 自动生成包含指标分析、测试数据说明、实验设计的标准化测试报告 |

---

## 4. 功能优先级

### 4.1 MVP（必须完成，对应初评核心指标）

> 目标：系统可运行，核心指标达标，能演示完整流程

| 优先级 | 功能 ID | 功能名称 | 原因 |
|---|---|---|---|
| P0 | F-01～F-04 | 数据接入、清洗、标准化、质量校验 | 所有模块的基础 |
| P0 | F-10、F-11 | 显式/隐式偏好抽取 | 评分项：偏好提取准确率≥85% |
| P0 | F-13 | 置信度评分 | 记忆质量核心机制 |
| P0 | F-20、F-21 | 知识抽取与结构化存储 | 知识记忆基础 |
| P0 | F-22 | 知识冲突处理 | 评分项：冲突处理正确率≥88% |
| P0 | F-30～F-33 | 混合检索 + 提示词注入 | 评分项：召回率≥85%，延迟≤500ms |
| P0 | F-40、F-41 | 敏感信息过滤 + 自然语言遗忘 | 比赛明确要求 |
| P0 | F-50、F-51 | 评测指标统计 + 测试数据集 | 评分项：性能指标（25分） |
| P0 | F-06 | 短/中/长期记忆流转兼容 | 比赛明确要求 |

### 4.2 Version 1（复评答辩加分项）

| 优先级 | 功能 ID | 功能名称 | 原因 |
|---|---|---|---|
| P1 | F-05 | 场景自动识别 | 提升偏好适配精度 |
| P1 | F-14、F-15 | 版本化管理 + 跨场景适配 | 体现技术深度 |
| P1 | F-16 | 偏好冲突检测 | 差异化亮点 |
| P1 | F-24 | 知识复用模板 | 实用性加分 |
| P1 | F-35 | 检索缓存 | 性能优化 |
| P1 | F-52、F-53 | 对比实验 + 自动测试报告 | 文档规范性加分（5分） |

### 4.3 Version 2（可选增强，突出创新性）

| 优先级 | 功能 ID | 功能名称 | 原因 |
|---|---|---|---|
| P2 | F-12 | 工具偏好深度分析 | 技术创新性加分（30分） |
| P2 | F-17 | 安全策略偏好专项管理 | 提升实用性 |
| P2 | F-25 | 知识老化管理 | 长期系统稳健性 |
| P2 | F-42、F-43 | 精准遗忘 + 审计日志 | 安全合规完整性 |
| P2 | F-34 | 安全规则强制注入 | 安全增强 |

---

## 5. 技术栈建议

### 5.1 核心技术栈

| 层次 | 技术选型 | 说明 |
|---|---|---|
| **API 服务** | FastAPI + Uvicorn | 轻量、高性能，适合端侧部署；自带 OpenAPI 文档 |
| **向量 Embedding** | 银河麒麟 embedding SDK | 比赛明确要求适配，必须使用 |
| **向量存储** | FAISS（本地） | 纯本地，无需网络，端侧友好；支持语义检索 |
| **关键词检索** | rank_bm25 | 轻量 BM25 实现，与向量检索融合 |
| **结构化存储** | SQLite | 事件记录、索引、元数据存储；无需独立数据库服务 |
| **长期记忆载体** | Markdown 文件 | 人类可读、可解释、便于演示和调试 |
| **LLM 调用** | 本地 Ollama（模拟）/ API | 用于偏好抽取、知识抽取、冲突判断；模拟 Agent 可用规则替代 |
| **敏感信息识别** | 正则规则 + 本地分类模型 | 不依赖外部服务，保证数据不出端侧 |
| **测试框架** | pytest | 单元测试、集成测试 |
| **Mock Agent** | Python CLI 脚本 | 模拟 Agent 运行，触发 Memory 的三个调用时机 |

### 5.2 目录结构

```
os-memory-agent/
  backend/
    main.py                  # FastAPI 入口
    api/
      schemas.py             # 请求/响应数据模型
      routes.py              # 路由注册
    memory/
      input_layer.py         # 数据接入、清洗、标准化
      extractor.py           # 偏好抽取、知识抽取
      conflict.py            # 冲突检测与合并
      store.py               # Markdown + SQLite 写入
      retriever.py           # 混合检索 + prompt_block 生成
      forget.py              # 自然语言遗忘
      evaluator.py           # 评测指标计算
  memory_store/
    users/
      u001/
        preferences.md
        knowledge.md
        workflows.md
        safety.md
        tools.md
        index.json
  demo/
    mock_agent.py            # 模拟 Agent 调用 Memory 完整流程
  data/
    test_cases.json          # 标准化测试数据集
    conflict_cases.json      # 冲突样例
    forget_cases.json        # 遗忘指令样例
  docs/
    architecture.md          # 系统架构文档
    api_reference.md         # API 参考文档
    user_manual.md           # 用户手册
    test_report.md           # 测试报告
  tests/
    test_extractor.py
    test_retriever.py
    test_conflict.py
    test_forget.py
  README.md
```

### 5.3 银河麒麟适配要点

- 调用麒麟 embedding SDK 替换通用 embedding 模型
- 打包时适配麒麟 KylinOS 的 Python 环境和依赖路径
- 提供麒麟系统下的安装部署脚本和适配测试报告

---

## 6. 非功能性需求

### 6.1 性能需求（比赛硬性指标）

| 指标 | 要求 | 实现策略 |
|---|---|---|
| 偏好提取准确率 | ≥ 85% | 多源证据融合 + 置信度阈值过滤 |
| 知识检索召回率 | ≥ 85% | BM25 + 向量混合检索 |
| 知识冲突处理正确率 | ≥ 88% | 规则引擎 + LLM 辅助判断 |
| 检索响应延迟 | ≤ 500ms | FAISS 本地索引 + 检索缓存 + 异步处理 |

### 6.2 安全性需求

- 所有数据在端侧本地处理，不向外部网络传输
- 敏感信息（手机号、身份证、密钥、密码）写入前自动识别并脱敏
- 遗忘操作不可逆，执行后目标记忆条目从所有存储层删除或失效
- 遗忘操作记录审计日志，保留操作时间、操作指令、被删除条目 ID

### 6.3 可靠性需求

- 记忆写入操作具备事务语义，失败时不产生部分写入
- 系统重启后长期记忆完整恢复（Markdown + SQLite 落盘）
- 向量索引支持增量更新，无需全量重建

### 6.4 可解释性需求

- 每条记忆含完整元数据：id、类型、场景、内容、置信度、来源会话、版本号、创建时间、更新时间
- 检索结果附带相关性得分和来源说明，便于调试和演示
- Markdown 格式长期记忆文件人类可读，支持手工审查和编辑

### 6.5 可维护性需求

- 源代码含完整注释，注释覆盖率 ≥ 80%
- 模块间通过标准接口解耦，Memory 模块可独立于 Agent 运行和测试
- 配置项（阈值、Top-K、场景列表等）集中管理，支持不修改代码调整参数

### 6.6 兼容性需求

- 支持银河麒麟桌面操作系统（KylinOS）运行
- Python ≥ 3.9，依赖库均可在麒麟系统 pip 环境中安装
- 提供麒麟系统适配测试报告

### 6.7 端侧资源约束

- 内存占用（运行时）：≤ 512MB
- 磁盘占用（完整部署）：≤ 2GB
- 无需 GPU，CPU 推理可达性能指标

---

## 7. 系统架构设计

### 7.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                   OS Agent（模拟）                │
│  接收用户输入 → 调用LLM → 调用工具 → 汇总轨迹   │
└─────────┬───────────────────────┬───────────────┘
          │ retrieve(query)       │ commit(trace) / ingest_event(event)
          ↓                       ↓
┌─────────────────────────────────────────────────┐
│              Memory API 层（FastAPI）             │
│  /memory/retrieve  /memory/events  /memory/commit│
│  /memory/forget    /memory/stats                 │
└────┬─────────────────────────────────────┬───────┘
     │                                     │
     ↓                                     ↓
┌──────────────┐                  ┌──────────────────┐
│  检索引擎    │                  │  写入处理流水线   │
│  BM25        │                  │  input_layer      │
│  FAISS向量   │                  │  extractor        │
│  混合融合    │                  │  conflict         │
│  缓存层      │                  │  store            │
└──────┬───────┘                  └────────┬─────────┘
       │                                   │
       ↓                                   ↓
┌──────────────────────────────────────────────────┐
│                    存储层                         │
│  Markdown（长期记忆，人类可读）                   │
│  SQLite（事件记录、索引、元数据）                 │
│  FAISS索引（向量，语义检索）                      │
└──────────────────────────────────────────────────┘
```

### 7.2 记忆处理流水线

```
Agent 提交数据
    ↓
input_layer：清洗 → 标准化 → 生成 raw_event → 写入 SQLite
    ↓
extractor：识别候选偏好/知识 → 置信度评分 → 生成 candidate
    ↓
conflict：检测冲突 → 选择合并策略 → 版本更新
    ↓
store：写入 Markdown + 更新 JSON 索引 + 更新 FAISS 向量
```

### 7.3 三类记忆数据流转

| 记忆层 | 生命周期 | 存储方式 | 与本系统关系 |
|---|---|---|---|
| 短期记忆 | 当前 session | Agent 内存 / 临时表 | 由 Agent 维护，session 结束后通过 `commit` 接口提交给本系统 |
| 中期记忆 | 最近 N 天 / N 次任务 | SQLite raw_events 表 | 本系统维护，支持时间窗口查询，可作为偏好抽取的滑动窗口数据源 |
| 长期记忆 | 永久（直至遗忘） | Markdown + FAISS | 本系统核心产物，为未来 session 提供持久化记忆 |

---

## 8. API 接口设计

### 8.1 POST /memory/retrieve — 检索记忆

**用途：** 任务开始前，Agent 检索与当前问题相关的记忆，注入提示词。

**请求：**
```json
{
  "user_id": "u001",
  "query": "帮我整理会议记录",
  "scene": "office",
  "top_k": 5
}
```

**响应：**
```json
{
  "preferences": ["用户偏好会议纪要使用表格形式输出。"],
  "knowledge": ["会议纪要模板包括：议题、结论、待办事项、负责人、截止时间。"],
  "workflows": ["会议纪要整理流程：读取文件 → 提取议题 → 汇总结论 → 生成表格"],
  "safety_rules": ["涉及公司内部文件时优先使用本地工具处理。"],
  "prompt_block": "...",
  "latency_ms": 120
}
```

---

### 8.2 POST /memory/events — 记录事件

**用途：** 工具调用后，记录原始事件到事件层。

**请求：**
```json
{
  "user_id": "u001",
  "session_id": "s001",
  "event_type": "tool_result",
  "tool_name": "export_document",
  "content": "已导出为 markdown 格式",
  "success": true,
  "output_format": "markdown",
  "latency_ms": 340
}
```

**响应：**
```json
{
  "event_id": "evt_20260613_001",
  "status": "recorded"
}
```

---

### 8.3 POST /memory/commit — 提交 session trace

**用途：** 任务结束后提交完整轨迹，触发长期记忆生成。

**请求：**
```json
{
  "user_id": "u001",
  "session_id": "s001",
  "user_message": "以后会议纪要都用表格输出",
  "llm_plan": "读取文件，提取议题、结论、待办事项",
  "tool_results": [
    {"tool_name": "read_file", "success": true},
    {"tool_name": "export_document", "success": true, "output_format": "markdown"}
  ],
  "final_answer": "已整理完成，输出为 Markdown 表格格式"
}
```

**响应：**
```json
{
  "new_memories": ["mem_pref_012"],
  "updated_memories": ["mem_pref_003"],
  "conflicts_resolved": 1
}
```

---

### 8.4 POST /memory/forget — 自然语言遗忘

**用途：** 根据自然语言指令删除或失效指定记忆。

**请求：**
```json
{
  "user_id": "u001",
  "instruction": "忘掉我关于使用 Word 格式的所有偏好"
}
```

**响应：**
```json
{
  "deleted_memory_ids": ["mem_pref_002", "mem_pref_007"],
  "status": "success"
}
```

---

### 8.5 GET /memory/stats — 评测指标

**用途：** 获取系统评测指标，用于测试报告。

**响应：**
```json
{
  "preference_accuracy": 0.91,
  "knowledge_recall": 0.87,
  "conflict_accuracy": 0.90,
  "avg_latency_ms": 210,
  "total_memories": 156,
  "total_events": 1024
}
```

---

## 9. 数据模型设计

### 9.1 正式记忆（Memory）

```json
{
  "memory_id": "mem_pref_001",
  "user_id": "u001",
  "memory_type": "preference",
  "key": "summary_output_format",
  "value": {
    "text_format": "markdown",
    "export_format": "pdf",
    "length": "one_page"
  },
  "scene": "office_summary",
  "confidence": 0.92,
  "source_events": ["evt_001", "evt_005"],
  "version": 2,
  "status": "active",
  "created_at": "2026-06-10T10:00:00",
  "updated_at": "2026-06-13T14:30:00"
}
```

### 9.2 原始事件（raw_event）

```json
{
  "event_id": "evt_001",
  "user_id": "u001",
  "session_id": "s001",
  "event_type": "user_message",
  "scene": "meeting_minutes",
  "content": "以后会议纪要都用表格输出",
  "raw_payload": { "original": "..." },
  "source": "agent",
  "timestamp": "2026-06-13T14:00:00"
}
```

### 9.3 记忆类型分类

| memory_type | 说明 | 存储文件 |
|---|---|---|
| preference | 用户偏好（输出格式、工具选择、风格习惯） | preferences.md |
| knowledge | 领域知识、工作流知识、历史案例 | knowledge.md |
| workflow | 可复用的任务执行流程模板 | workflows.md |
| safety | 安全约束、隐私规则（强制注入） | safety.md |
| tool | 工具经验（成功率、偏好、失败原因） | tools.md |

---

## 10. 评测指标与测试方案

### 10.1 核心指标（比赛评分项）

| 指标 | 目标值 | 测试方法 |
|---|---|---|
| 偏好提取准确率 | ≥ 85% | 人工标注 50 条用户输入，与系统提取结果对比 |
| 知识检索召回率 | ≥ 85% | 构造 50 条查询 + 标准答案集，计算召回率 |
| 知识冲突处理正确率 | ≥ 88% | 构造 30 对冲突知识样例，验证处理结果 |
| 检索响应延迟 | ≤ 500ms | 100 次检索请求取 P95 延迟 |

### 10.2 测试数据集构成

```
data/
  test_cases.json          # 偏好抽取测试：输入 + 期望输出
  retrieval_cases.json     # 检索召回测试：query + 标准答案
  conflict_cases.json      # 冲突处理测试：冲突对 + 期望合并结果
  forget_cases.json        # 遗忘测试：指令 + 目标 memory_id
  session_traces.json      # 完整 session 轨迹样例
```

### 10.3 对比实验设计

| 实验组 | 描述 | 目的 |
|---|---|---|
| 基线组 | Agent 无记忆，每次 session 从零开始 | 对照 |
| 实验组 | Agent 接入本记忆系统 | 量化记忆增益 |
| 评估维度 | 任务完成质量、偏好符合率、用户满意度模拟得分 | — |

### 10.4 真实场景应用案例

**场景：会议纪要智能整理**

1. 用户第一次提交会议记录，明确表示"以后用表格格式"
2. Memory 写入偏好记忆：`meeting_minutes_format = table`
3. 用户第二次提交会议记录（不再说明格式）
4. Agent 检索记忆，自动注入"表格格式"偏好
5. LLM 直接输出符合用户习惯的表格格式纪要

**验证点：** 第二次任务是否自动应用偏好，无需用户重复说明。

---

## 11. 比赛评分要点对照

### 11.1 初评评分对照（100分）

| 评分项 | 分值 | 对应功能 / 设计 |
|---|---|---|
| **技术创新性** | 30分 | 多源证据融合置信度评分（F-13）；偏好冲突动态检测（F-16）；混合检索融合策略（F-32）；自然语言遗忘（F-41）；跨场景偏好适配（F-15） |
| **方案可行性** | 30分 | FastAPI + SQLite + FAISS 轻量化架构；银河麒麟 embedding SDK 适配；Mock Agent 完整演示；量化论证数据（测试报告） |
| **性能指标** | 25分 | 偏好提取≥85%；召回率≥85%；冲突处理≥88%；延迟≤500ms（见第10节） |
| **实用性** | 10分 | 会议纪要、周报、项目总结等真实场景案例；直接解决 OS Agent 个性化服务痛点 |
| **文档规范性** | 5分 | 完整 PRD、架构文档、API 文档、用户手册、测试报告 |

### 11.2 复评评分对照（100分）

| 评分项 | 分值 | 准备要点 |
|---|---|---|
| **设计新颖性** | 10分 | 重点阐述：多源融合机制、自然语言遗忘、端侧轻量化设计 |
| **功能实现完整性** | 60分 | 现场演示完整流程：数据写入 → 检索 → 注入提示词 → 遗忘 |
| **应用价值** | 10分 | 强调：降低端侧资源占用、适用办公/协作/个人助手多场景 |
| **表述能力** | 20分 | 参考知识点第20条的标准口径；流程图辅助讲解 |

### 11.3 对评委的标准口径

> 我们的记忆系统不依赖完整 OS Agent，而是通过标准化接口接收 Agent 运行轨迹。任务开始时，Agent 以用户当前问题作为 query 调用记忆检索接口，记忆系统返回相关的偏好、知识、流程和安全规则，Agent 将这些记忆作为上下文注入 LLM 提示词。任务执行中，工具调用结果以 event 形式进入事件层。任务结束后，Agent 提交完整 session trace，记忆系统从用户原始输入、LLM 规划、工具结果和最终输出中抽取候选记忆，经过敏感信息过滤、置信度评分、冲突检测和版本合并后，形成可长期存储和复用的偏好记忆、知识记忆、流程记忆、工具记忆和安全记忆，检索响应时间控制在 500ms 以内。

---

## 12. 提交物清单

根据比赛要求（2026年9月15日截止），需提交以下材料：

| 提交物 | 格式 | 负责模块 |
|---|---|---|
| 项目报告（PPT） | .ppt | 包含方案设计、架构图、演示截图、指标数据 |
| 技术方案及测试结果 | .word / .pdf | 需求分析、架构设计、核心算法、安装部署、测试报告 |
| 效果演示视频 | 视频 | 覆盖偏好捕捉、知识整合、检索复用、遗忘完整流程 |
| 用户手册 | .pdf | 安装、配置、使用说明 |
| 源代码及注释 | .zip | 含 README，注释完整，无知识产权纠纷 |
| 麒麟适配测试报告 | .pdf | 在银河麒麟系统上的安装和功能验证记录 |
| 参赛报名表（盖章） | 扫描件 | — |

**提交邮箱：** wangyu1@kylinos.cn  
**命名格式：** `提报学校全称－OS Agent 记忆优化及高效应用研究－作品名称`

---

## 13. 里程碑与时间规划

| 阶段 | 时间 | 目标 |
|---|---|---|
| **阶段一：基础框架** | 6月下旬 | FastAPI 服务跑通；数据接入、标准化完成；Markdown 存储可读写 |
| **阶段二：核心算法** | 7月 | 偏好抽取、知识抽取、冲突处理、混合检索完成；指标初步达标 |
| **阶段三：完整闭环** | 8月 | Mock Agent 演示跑通完整流程；遗忘、敏感过滤完成；测试数据集构造完毕 |
| **阶段四：优化与文档** | 9月1日～9月14日 | 性能调优（延迟、准确率）；完成所有文档；麒麟适配测试；录制演示视频 |
| **截止提交** | 2026年9月15日 | 全量材料提交至 wangyu1@kylinos.cn |
| **初审结果** | 2026年9月30日前 | 等待初审，准备复评答辩 |
| **复评答辩** | 2026年11月 | 终审擂台赛 |
